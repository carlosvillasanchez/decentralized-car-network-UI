/* eslint-disable */
class CarCrash {
    constructor(
        id,
        x,
        y,
        intialTime,
        maxTime
    ) {
        this.id = id;
        this.x = x;
        this.y = y;
        this.intialTime = intialTime;
        this.maxTime = maxTime
    }
}

export default CarCrash;